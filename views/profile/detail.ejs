<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Profile Detail — LI Scraper</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;600&family=Syne:wght@400;600;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/app.css" />
</head>
<body>
  <%- include('../partials/sidebar', { page: 'profile-detail' }) %>
  <main class="main">
    <header class="topbar">
      <h1 class="topbar-title">Profile Detail</h1>
      <div class="topbar-meta"><code class="endpoint-badge">GET /profile/detail</code></div>
    </header>
    <div class="content">
      <div class="card">
        <div class="card-title">Parameters</div>
        <form method="POST">
          <div class="form-row">
            <div class="form-group">
              <label for="username">LinkedIn Profile URL</label>
              <input type="text" id="profile_url" name="profile_url" value="<%= profile_url %>" placeholder="https://www.linkedin.com/in/neal-mohan" required />
            </div>
            <button type="submit" class="btn btn-primary">Fetch →</button>
          </div>
        </form>
      </div>
      <%- include('../partials/result', { result, status, error }) %>
    </div>
  </main>
  <script src="/js/app.js"></script>
</body>
</html>
